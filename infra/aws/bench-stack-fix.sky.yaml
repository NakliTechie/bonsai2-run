# Leg 1 of the n-gram + DFlash2 stacking study: 4x L4 (g6.12xlarge), thinking off. llama.cpp already stacks
# speculators by priority (ngram-* first, draft models as fallback), so this is measurement only.
# Pass A: copy-heavy sets (Spec-Bench summarization/RAG, code-edit, MT-Bench turn 2) x {plain, dflash, ngram, stack, stackloose}.
# Pass B: the original 5 sets x {dflash, stack, stackloose} on the same box, to catch any regression from ngram priority.
# Every pass runs INSIDE the run job (infra/aws README gotcha 2026-09-24). cairn=true is the IAM destructive gate.
name: bonsai2-run-bench-stack-fix

resources:
  cloud: aws
  disk_size: 150
  labels:
    cairn: "true"
    cairn-purpose: "bonsai2-run"
  any_of:
    - {region: us-east-2, instance_type: g6.12xlarge, use_spot: false}
    - {region: us-west-2, instance_type: g6.12xlarge, use_spot: false}
    - {region: eu-south-2, instance_type: g6.12xlarge, use_spot: false}
    - {region: ap-northeast-2, instance_type: g6.12xlarge, use_spot: false}
    - {region: ap-south-1, instance_type: g6.12xlarge, use_spot: false}

workdir: .

setup: |
  set -uo pipefail
  sudo shutdown -h +240 'bench-stack hard deadline'   # backstop: 4 h after setup starts
  nvidia-smi --query-gpu=name,driver_version,memory.total --format=csv,noheader
  docker info --format '{{.ServerVersion}} runtimes={{json .Runtimes}}' 2>&1 | head -1
  if ! docker run --rm --gpus all nvidia/cuda:12.8.1-base-ubuntu24.04 nvidia-smi -L; then
    echo ">>> installing nvidia-container-toolkit"
    curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | sudo gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
    curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
    sudo apt-get update -qq && sudo apt-get install -y -qq nvidia-container-toolkit && sudo nvidia-ctk runtime configure --runtime=docker && sudo systemctl restart docker
    docker run --rm --gpus all nvidia/cuda:12.8.1-base-ubuntu24.04 nvidia-smi -L
  fi
  curl -LsSf https://astral.sh/uv/install.sh | sh > /dev/null 2>&1; export PATH="$HOME/.local/bin:$PATH"
  uv tool install -q "huggingface_hub[cli]" >/dev/null 2>&1; uv tool install -q awscli >/dev/null 2>&1
  uv venv -q ~/venv --python 3.12 && uv pip install -q --python ~/venv/bin/python datasets >/dev/null 2>&1
  ~/venv/bin/python scripts/evalset.py ~/sets && python3 scripts/evalset_t2.py ~/sets results/bench-2026-09-24/PTQ1_0-plain/off/mtbench.jsonl && docker pull -q python:3.12-slim >/dev/null
  mkdir -p ~/models
  ( hf download prism-ml/Ternary-Bonsai-2-27B-gguf Ternary-Bonsai-2-27B-PTQ1_0.gguf Ternary-Bonsai-2-27B-PQ2_0.gguf --local-dir ~/models >/dev/null 2>&1 \
    && hf download naklitechie/Qwen3.8-27B-DFlash2-ternary-bonsai2 Qwen3.8-27B-DFlash2-r3-Q4_K_M.gguf --local-dir ~/models >/dev/null 2>&1 \
    && hf download z-lab/Qwen3.8-27B-DFlash2-GGUF Qwen3.8-27B-DFlash2-Q4_K_M.gguf --local-dir ~/models >/dev/null 2>&1 \
    && echo ">>> models ready: $(ls -la ~/models/*.gguf | awk '{print $5, $9}' | tr '\n' ' ')" ) &
  DL=$!
  t=$(date +%s); docker build -t bonsai2-run:dev . > ~/build.log 2>&1 && echo ">>> image built in $(( $(date +%s) - t ))s: $(docker image ls bonsai2-run:dev --format '{{.Size}}')" || { echo ">>> BUILD FAILED"; tail -40 ~/build.log; }
  wait $DL
  df -h / | tail -1

run: |
  # Re-runs the two pass-A units lost on 2026-09-24 16:47 (pass B's AFTER_LOG matched a stale "gpu1: queue empty"
  # from the aborted job 1, so both passes used container b2r1 / port 8091 at once). Fresh TAG, no AFTER_LOG.
  set -uo pipefail
  export PATH="$HOME/.local/bin:$PATH"; OUT=results/bench-stack-2026-09-24; export OUT
  docker image inspect bonsai2-run:dev >/dev/null 2>&1 || { echo "NO IMAGE"; exit 1; }
  rm -f $OUT/.queue-fix*
  TAG=-fix UNITS="PQ2_0 dflash off;PQ2_0 stackloose off" OFF_LIMITS="sb_sum:80 sb_rag:80 codeedit:80 mtbench_t2:80" bash scripts/evalbox.sh
  ~/venv/bin/python scripts/score.py ~/sets $OUT > $OUT/score.out 2>&1; tail -45 $OUT/score.out
  aws s3 sync $OUT s3://skypilot-cairn-artifacts/bonsai2-run/$(basename $OUT) --only-show-errors
  echo "BENCH-STACK-FIX DONE"
